function add(t1, t2) {
    return t1;
}
function compareNumbers(a, b) {
    if (typeof a === "number" && typeof b === "number") {
        if (a === b) {
            return true;
        }
    }
    else {
        // compareArays
        // const result = compareArrays()
        let result = true;
        return result;
    }
}
